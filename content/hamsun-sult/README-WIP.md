# hamsun-sult — UFERDIG IMPORT, ikke merget

Kilde, `segments.json`, `original.json` og `pack.json` er på plass og validerer.
Det som mangler er `rules.v1.json` og treningsutgaven, så
**`pnpm validate:content` melder «[hamsun-sult] no training edition»** — det er
det eneste problemet som står igjen på denne branchen.

Se køen / handover for hva som gjenstår. Slett denne filen når pakken er hel.
